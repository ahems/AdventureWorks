# Environment variables for the AdventureWorks App

# GraphQL API URL for local development
VITE_API_URL=http://localhost:5000/graphql

# Note: In production, API_URL is provided by Azure Container App environment
# and injected at runtime via window.APP_CONFIG by the Docker entrypoint
