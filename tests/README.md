# Integration Test Scripts

This directory contains API and integration test scripts for the AdventureWorks application. The scripts in `tests/scripts/` exercise deployed APIs and Azure Functions directly with `curl` and `jq`.

Run the scripts from the repository root after provisioning an Azure environment with `azd up`.
